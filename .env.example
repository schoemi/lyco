# Database
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/lyco?schema=public"

# NextAuth.js (v5)
AUTH_SECRET="change-me-to-a-random-secret"
AUTH_URL="http://localhost:3000"
AUTH_COOKIE_SECURE=true          # Set to "false" for HTTP-only staging environments
# Legacy aliases (still supported)
NEXTAUTH_SECRET="change-me-to-a-random-secret"
NEXTAUTH_URL="http://localhost:3000"

# OpenAI / LLM
OPENAI_API_KEY=sk-...          # Required for PDF song import
LLM_API_KEY=sk-...             # Required: API key for the LLM provider
LLM_API_URL=                   # Optional: Base URL for the LLM API (defaults to OpenAI)
LLM_MODEL=                     # Optional: Model name (defaults to gpt-4o-mini)

# Genius Song-Import
GENIUS_ENCRYPTION_KEY=           # Required for Genius import: 32-byte hex-encoded key (64 hex characters). Generate with: openssl rand -hex 32

# Docker Deployment (docker-compose)
APP_PORT=3000                  # Optional: Host port for the app (default: 3000)
DB_PORT=5434                   # Optional: Host port for PostgreSQL (default: 5434)
APP_IMAGE=ghcr.io/schoemi/lyco:latest  # Optional: Override Docker image

# WebAuthn Configuration (required for passkey features)
WEBAUTHN_RP_ID=localhost                # Relying Party ID (domain)
WEBAUTHN_RP_NAME=Lyco                   # Human-readable RP name
WEBAUTHN_ORIGIN=http://localhost:3000   # Expected origin for WebAuthn

# SSO Configuration (all required for SSO to be active)
SSO_CLIENT_ID=
SSO_CLIENT_SECRET=
SSO_ISSUER_URL=

# E-Mail / SMTP
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=noreply@lyco.app
